# Frontend — imagem de desenvolvimento (hot reload via next dev)
# Só instala dependências; o código-fonte vem via volume no compose
FROM node:22-alpine
WORKDIR /app
COPY frontend/package.json ./
RUN npm install
EXPOSE 3000
CMD ["npm", "run", "dev"]
